import { ServiceJavaService } from './service-java-service';

describe('ServiceJavaService', () => {
  it('should create an instance', () => {
    expect(new ServiceJavaService()).toBeTruthy();
  });
});
